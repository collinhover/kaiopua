/*
Camera.js
Camera module, handles generating cameras in game.
*/
var KAIOPUA = (function (main) {
    
    var shared = main.shared = main.shared || {},
        game = main.game = main.game || {},
		core = game.core = game.core || {},
		camera = game.camera = game.camera || {};
	
	/*===================================================
    
    public properties
    
    =====================================================*/
	
	camera.instantiate = instantiate;
	
	/*===================================================
    
    camera
    
    =====================================================*/
	
	function instantiate () {
		
		var c = {};
		
		
		
		return c;
		
	}
	
	return main;
	
}(KAIOPUA || {}));